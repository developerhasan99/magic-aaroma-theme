{%- style -%}
  .section-{{ section.id }}-padding {
    padding-top: {{ section.settings.padding_top | times: 0.75 | round: 0 }}px;
    padding-bottom: {{ section.settings.padding_bottom | times: 0.75 | round: 0 }}px;
  }

  @media screen and (min-width: 750px) {
    .section-{{ section.id }}-padding {
      padding-top: {{ section.settings.padding_top }}px;
      padding-bottom: {{ section.settings.padding_bottom }}px;
    }
  }
{%- endstyle -%}

<div
  id="{{ section.id }}"
  class="section-{{ section.id }}-padding"
  style="background-color: {{ section.settings.background_color }}">
  <div class="page-width">
    <div class="max-w-2xl mx-auto">
      <div class="text-center mb-8">
        <h2 class="my-0 text-2xl md:text-3xl font-bold text-gray-900">
          {{ section.settings.title }}
        </h2>

      </div>
      <div class="">

        <!-- Header -->
        <div class="flex items-center justify-between">
          <!-- Logo -->
          <div>
            <div class="text-2xl font-bold tracking-widest text-gray-900 leading-none">MAGIC</div>
            <div class="text-xs tracking-[0.35em] text-gray-500 font-medium mt-0.5">PERFUME</div>
          </div>
          <!-- Perfume bottle -->
          <div class="relative">
            <img
              class="w-24 h-auto"
              height=""
              width=""
              src="https://cdn.shopify.com/s/files/1/0700/7928/3458/files/other-bottle_ba83590b-69af-48a5-95d1-fdbd6f31e25f.png?v=1774461493&width=300"
              alt="Other brand">
          </div>
        </div>

        <!-- Comparison Rows -->
        <div class="flex flex-col gap-2.5">
          {% assign comparison_features = "Hoge kwaliteit,Langhoudend,Voordelig,Dermatologisch getest,Combineerbare geuren,Gratis retour binnen 30 dagen,Snelle verzending,100% vegan,Dierproefvrij" | split: "," %}
          {% assign magic_statuses = "check,check,check,check,check,check,check,check,check" | split: "," %}
          {% assign other_statuses = "check,warning,cross,check,cross,warning,warning,warning,warning" | split: "," %}
          {% assign other_texts = " ,Afhankelijk van<br>verkoper, , , ,Zelden,Afhankelijk van<br>verkoper,Niet<br>altijd,Niet<br>altijd" | split: "," %}

          {% for feature_name in comparison_features %}
            {% assign current_index = forloop.index0 %}
            {% assign magic_status = magic_statuses[current_index] %}
            {% assign other_status = other_statuses[current_index] %}
            {% assign other_text = other_texts[current_index] %}

              <div class="grid grid-cols-[44px_1fr_80px] items-center gap-x-3 bg-gray-100 rounded-full px-3 py-2">
              <!-- Magic Perfume Status -->
              <div class="flex items-center justify-center">
                {% if magic_status == 'check' %}
                  {% render 'icon-check-green' %}
                {% endif %}
              </div>

              <!-- Feature Name -->
              <div class="text-center text-gray-800 font-medium text-[15px] bg-white rounded-full py-1">{{ feature_name }}</div>

              <!-- Other Brands Status -->
              <div class="flex items-center justify-center">
                {% if other_status == 'check' %}
                  {% render 'icon-check-green' %}
                {% elsif other_status == 'cross' %}
                  {% render 'icon-check-red' %}
                {% elsif other_status == 'warning' %}
                  <div class="flex items-center justify-center gap-1">
                    {% render 'icon-warning' %}
                    <span class="text-xs text-gray-600 font-medium leading-tight text-center">{{ other_text }}</span>
                  </div>
                {% endif %}
              </div>
            </div>
          {% endfor %}
        </div>
      </div>
    </div>
  </div>
</div>

{% schema %}
  {
    "name": "Draft Comparison",
    "tag": "section",
    "settings": [
      {
        "type": "color",
        "id": "background_color",
        "label": "Background Color",
        "default": "#fcfbf9"
      },
      {
        "type": "text",
        "id": "title",
        "label": "Title",
        "default": "Waarom Magic Parfum?"
      },
      {
        "type": "text",
        "id": "subtitle",
        "label": "Subtitle",
        "default": "Het slimme alternatief voor overpriced luxemerken."
      },
      {
        "type": "header",
        "content": "Comparison Items"
      }, {
        "type": "textarea",
        "id": "comparison_items",
        "label": "Comparison Items (JSON format)",
        "default": "[{\"feature_name\":\"Hoge kwaliteit\",\"magic_status\":\"check\",\"other_status\":\"check\"},{\"feature_name\":\"Langhoudend\",\"magic_status\":\"check\",\"other_status\":\"warning\",\"other_text\":\"Afhankelijk van<br>verkoper\"},{\"feature_name\":\"Voordelig\",\"magic_status\":\"check\",\"other_status\":\"cross\"},{\"feature_name\":\"Dermatologisch getest\",\"magic_status\":\"check\",\"other_status\":\"check\"},{\"feature_name\":\"Combineerbare geuren\",\"magic_status\":\"check\",\"other_status\":\"cross\"},{\"feature_name\":\"Gratis retour binnen 30 dagen\",\"magic_status\":\"check\",\"other_status\":\"warning\",\"other_text\":\"Zelden\"},{\"feature_name\":\"Snelle verzending\",\"magic_status\":\"check\",\"other_status\":\"warning\",\"other_text\":\"Afhankelijk van<br>verkoper\"},{\"feature_name\":\"100% vegan\",\"magic_status\":\"check\",\"other_status\":\"warning\",\"other_text\":\"Niet<br>altijd\"},{\"feature_name\":\"Dierproefvrij\",\"magic_status\":\"check\",\"other_status\":\"warning\",\"other_text\":\"Niet<br>altijd\"}]"
      }, {
        "type": "header",
        "content": "t:sections.all.padding.section_padding_heading"
      }, {
        "type": "range",
        "id": "padding_top",
        "min": 0,
        "max": 100,
        "step": 4,
        "unit": "px",
        "label": "t:sections.all.padding.padding_top",
        "default": 36
      }, {
        "type": "range",
        "id": "padding_bottom",
        "min": 0,
        "max": 100,
        "step": 4,
        "unit": "px",
        "label": "t:sections.all.padding.padding_bottom",
        "default": 36
      }
    ],
    "presets": [
      {
        "name": "Draft Comparison"
      }
    ]
  }
{% endschema %}